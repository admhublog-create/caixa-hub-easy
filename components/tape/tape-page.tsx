"use client";import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";import {Card,CardContent} from "@/components/ui/card";import {Input} from "@/components/ui/input";import {Button} from "@/components/ui/button";import {KpiCard} from "@/components/ui/kpi-card";import {CircleDot,History,PackageOpen} from "lucide-react";import {TapeWithdrawal} from "./tape-withdrawal";import {TapeInventory} from "./tape-inventory";const BASE=143,PER_BOX=15;type R={created_at:string;rolos:number};type E={created_at:string;total_rolos:number};type I={created_at:string;total_rolos:number};export function TapePage(){const[rr,setR]=useState<R[]>([]),[ee,setE]=useState<E[]>([]),[inv,setInv]=useState<I|null>(null),[boxes,setBoxes]=useState("0"),[rolls,setRolls]=useState("0");async function load(){if(!supabase)return;const[r,e,i]=await Promise.all([supabase.from("fita_retiradas").select("created_at,rolos").order("created_at",{ascending:false}),supabase.from("fita_entradas").select("created_at,total_rolos").order("created_at",{ascending:false}),supabase.from("fita_inventarios").select("created_at,total_rolos").order("data",{ascending:false}).order("created_at",{ascending:false}).limit(1)]);setR((r.data||[]) as R[]);setE((e.data||[]) as E[]);setInv((i.data?.[0]||null) as I|null)}useEffect(()=>{load()},[]);const cut=inv?new Date(inv.created_at):null,base=inv?Number(inv.total_rolos):BASE,used=rr.filter(x=>!cut||new Date(x.created_at)>cut).reduce((s,x)=>s+Number(x.rolos||0),0),added=ee.filter(x=>!cut||new Date(x.created_at)>cut).reduce((s,x)=>s+Number(x.total_rolos||0),0),stock=base+added-used;async function add(){const cx=Math.max(0,Number(boxes)||0),av=Math.max(0,Number(rolls)||0),total=cx*PER_BOX+av;if(!supabase||!total)return;await supabase.from("fita_entradas").insert({caixas:cx,rolos_avulsos:av,total_rolos:total});setBoxes("0");setRolls("0");await load()}return <><div className="mb-6"><h2 className="text-2xl font-bold">Fita Gomada</h2><p className="text-sm text-[var(--muted)]">Controle de estoque físico. 1 caixa = 15 rolos.</p></div><section className="grid gap-4 md:grid-cols-3"><KpiCard title="Estoque atual" value={stock} detail="rolos disponíveis" Icon={CircleDot}/><KpiCard title="Entradas" value={ee.length} detail="movimentações físicas" Icon={PackageOpen}/><KpiCard title="Retiradas" value={rr.length} detail="movimentações registradas" Icon={History}/></section><div className="mt-6 grid gap-6"><TapeWithdrawal onSaved={load}/><Card><CardContent><h3 className="mb-1 font-bold">Nova entrada de fita gomada</h3><p className="mb-4 text-sm text-[var(--muted)]">Esta operação altera o estoque físico.</p><div className="grid gap-3 md:grid-cols-[1fr_1fr_auto]"><Input type="number" min="0" value={boxes} onChange={e=>setBoxes(e.target.value)} placeholder="Caixas fechadas"/><Input type="number" min="0" value={rolls} onChange={e=>setRolls(e.target.value)} placeholder="Rolos avulsos"/><Button onClick={add}>Registrar entrada</Button></div><p className="mt-3 text-sm font-medium">Total da entrada: {(Number(boxes)||0)*PER_BOX+(Number(rolls)||0)} rolos</p></CardContent></Card><TapeInventory stock={stock} onSaved={load}/></div></>}