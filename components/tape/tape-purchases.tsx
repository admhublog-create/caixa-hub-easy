"use client";import {useState} from "react";import {supabase} from "@/lib/supabase";import {Card,CardContent} from "@/components/ui/card";import {Input} from "@/components/ui/input";import {Button} from "@/components/ui/button";export function TapePurchases({onSaved}:{onSaved:()=>void}){const[month,setMonth]=useState(""),[kg,setKg]=useState(""),[boxes,setBoxes]=useState("0"),[rolls,setRolls]=useState("0"),[value,setValue]=useState(""),[supplier,setSupplier]=useState(""),[note,setNote]=useState("");async function save(){if(!supabase||!month)return;const cx=Math.max(0,Number(boxes)||0);let rl=Math.max(0,Number(rolls)||0);if(!rl&&cx)rl=cx*15;await supabase.from("fita_compras_historicas").insert({competencia:month,kg:Number(kg)||null,caixas:cx,rolos:rl,valor:Number(value)||null,fornecedor:supplier.trim()||null,observacao:note.trim()||null});setMonth("");setKg("");setBoxes("0");setRolls("0");setValue("");setSupplier("");setNote("");onSaved()}return <Card><CardContent><h3 className="font-bold">Compra histórica de fita</h3><p className="mb-4 text-sm text-[var(--muted)]">Registro financeiro e histórico. Não altera o estoque físico.</p><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4"><Input type="month" value={month} onChange={e=>setMonth(e.target.value)}/><Input type="number" step="0.001" value={kg} onChange={e=>setKg(e.target.value)} placeholder="Peso líquido (kg)"/><Input type="number" min="0" value={boxes} onChange={e=>setBoxes(e.target.value)} placeholder="Caixas"/><Input type="number" min="0" value={rolls} onChange={e=>setRolls(e.target.value)} placeholder="Rolos"/><Input type="number" step="0.01" value={value} onChange={e=>setValue(e.target.value)} placeholder="Valor total"/><Input value={supplier} onChange={e=>setSupplier(e.target.value)} placeholder="Fornecedor"/><Input value={note} onChange={e=>setNote(e.target.value)} placeholder="Observação"/><Button onClick={save}>Salvar compra</Button></div></CardContent></Card>}