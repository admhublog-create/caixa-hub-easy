import {Card,CardContent} from "./card";import {LucideIcon} from "lucide-react";export function KpiCard({title,value,detail,Icon}:{title:string;value:string|number;detail?:string;Icon:LucideIcon}){return <Card><CardContent className="flex items-start gap-4"><div className="grid size-11 place-items-center rounded-xl bg-[var(--primary-soft)] text-[var(--primary)]"><Icon size={22}/></div><div><p className="text-sm text-[var(--muted)]">{title}</p><strong className="mt-1 block text-2xl">{value}</strong>{detail&&<p className="mt-1 text-xs text-[var(--muted)]">{detail}</p>}</div></CardContent></Card>}