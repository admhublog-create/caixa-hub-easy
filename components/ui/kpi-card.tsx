import {Card,CardContent} from "./card";import type {LucideIcon} from "lucide-react";export function KpiCard({title,value,detail,Icon}:{title:string;value:string|number;detail?:string;Icon:LucideIcon}){return <Card className="overflow-hidden"><CardContent className="relative flex min-h-[132px] items-start gap-4"><div className="grid size-12 shrink-0 place-items-center rounded-xl bg-[var(--primary-soft)] text-[var(--primary)] ring-1 ring-emerald-700/5"><Icon size={22}/></div><div className="min-w-0"><p className="truncate text-sm font-medium text-[var(--muted)]">{title}</p><strong className="mt-1.5 block text-3xl font-bold tracking-tight text-gray-900">{value}</strong>{detail&&<p className="mt-1.5 truncate text-xs text-[var(--muted)]">{detail}</p>}</div><span className="absolute inset-x-0 bottom-0 h-1 bg-[var(--primary)]/80"/></CardContent></Card>}