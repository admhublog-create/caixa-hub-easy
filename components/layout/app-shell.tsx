"use client";import {useState,type ReactNode} from "react";import {Sidebar} from "./sidebar";import {Header} from "./header";export function AppShell({children}:{children:ReactNode}){const[open,setOpen]=useState(false);return <div><Sidebar mobileOpen={open} onClose={()=>setOpen(false)}/><div className="min-h-screen lg:pl-64"><Header onMenu={()=>setOpen(true)}/>{children}</div></div>}