"use client";import {useEffect,useState,type ReactNode} from "react";import {Sidebar} from "./sidebar";import {Header} from "./header";export function AppShell({children}:{children:ReactNode}){const[open,setOpen]=useState(false);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <div><Sidebar mobileOpen={open} onClose={()=>setOpen(false)}/><div className="min-h-screen lg:pl-[272px]"><Header onMenu={()=>setOpen(true)}/>{children}</div></div>}